# Producer-Consumer-Problem-in-OS
A C++ implementation of the classic Producer-Consumer problem, demonstrating issues arising from unsynchronized access to a shared buffer and providing a basic synchronized solution to prevent race conditions, buffer overflow, and underflow.
